<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Cases Index</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            line-height: 1.6;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #007acc;
            padding-bottom: 10px;
        }
        .test-suite {
            margin: 20px 0;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: #fafafa;
        }
        .test-suite h2 {
            margin-top: 0;
            color: #007acc;
        }
        .test-suite a {
            text-decoration: none;
            color: #007acc;
            font-weight: bold;
        }
        .test-suite a:hover {
            text-decoration: underline;
        }
        .summary {
            background: #e3f2fd;
            border-left: 4px solid #2196f3;
            padding: 15px;
            margin-bottom: 20px;
        }
        .stats {
            color: #666;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <h1>Test Cases Index</h1>

    <div class="summary">
        <strong>Total test suites:</strong> {{ test_suites|length }}
    </div>

    {% for name, path in test_suites %}
    <div class="test-suite">
        <h2>{{ name }}</h2>
        <p>
            <a href="{{ name }}/test_cases.html">View test cases</a>
        </p>
        <p class="stats">Path: {{ path }}</p>
    </div>
    {% endfor %}
</body>
</html>